import {ActionsType, StateType} from './state';

const initialState = {}

const sideBarReducer = (state: any = initialState, action: ActionsType): StateType => {
    return state
}

export default sideBarReducer